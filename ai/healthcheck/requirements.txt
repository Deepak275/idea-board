# ai/healthcheck runtime dependencies.
# The official Anthropic Python SDK (used via client.messages.create) and
# jsonschema for the authoritative propose->VALIDATE->approve guardrail.
anthropic>=0.40.0,<2.0.0
jsonschema>=4.18.0,<5.0.0
