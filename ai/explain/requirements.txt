# ai/explain runtime dependencies.
# Official Anthropic Python SDK + jsonschema (validate the structured result before
# trusting the destructive-changes flag).
anthropic>=0.40.0,<2.0.0
jsonschema>=4.18.0,<5.0.0
