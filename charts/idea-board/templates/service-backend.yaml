apiVersion: v1
kind: Service
metadata:
  # Contract-mandated name: "idea-board-backend".
  name: {{ include "idea-board.backend.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "idea-board.backend.labels" . | nindent 4 }}
  {{- with .Values.backend.service.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  type: {{ .Values.backend.service.type }}
  selector:
    {{- include "idea-board.backend.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.backend.service.port }}
      targetPort: http
      protocol: TCP
